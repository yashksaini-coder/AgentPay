services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: agentic_payments
      POSTGRES_USER: agent
      POSTGRES_PASSWORD: agent
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]

  anvil:
    image: ghcr.io/foundry-rs/foundry:latest
    entrypoint: ["anvil", "--host", "0.0.0.0"]
    ports: ["8545:8545"]

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - API_HOST=0.0.0.0
      - CORS_ORIGINS=http://localhost,http://app.agentpay.io
    ports:
      - "8080:8080"
      - "9000:9000"
    depends_on:
      - postgres
      - anvil

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    environment:
      - NEXT_PUBLIC_API_URL=/api
    ports:
      - "3000:3000"
    depends_on:
      - backend

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - frontend
      - backend

volumes:
  pgdata:
